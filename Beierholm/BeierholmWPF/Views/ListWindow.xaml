<Window x:Class="BeierholmWPF.ListWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:BeierholmWPF"
        mc:Ignorable="d"
        Title="Beierholm" Height="430" Width="550" ResizeMode="NoResize" WindowStartupLocation="CenterScreen">
    <Grid>
        <Grid.ColumnDefinitions>
            <ColumnDefinition Width="47*"/>
            <ColumnDefinition Width="53*"/>
        </Grid.ColumnDefinitions>
        <Grid.RowDefinitions>
            <RowDefinition Height="263*"/>
            <RowDefinition Height="151*"/>
        </Grid.RowDefinitions>
        <!-- CVR: 26550688 -->
        <!-- Labels -->
        <Label Content="CVR" Margin="71,47,71,0" Height="27" VerticalAlignment="Top" />
        <Label Content="Start Dato" HorizontalAlignment="Left" Margin="168,47,0,0" Height="27" VerticalAlignment="Top"/>
        <Label Content="Slut Dato" Grid.Column="1" HorizontalAlignment="Left" Margin="45,47,0,0" Height="27" VerticalAlignment="Top"/>
        <Label x:Name="ResultLabel" Content="Resultat for søgt: 'result'" HorizontalAlignment="Left" Margin="112,10,0,0" VerticalAlignment="Top" Width="154" Grid.ColumnSpan="2"/>
        <Label Content="EInkomst Oprettet" Grid.Column="1" HorizontalAlignment="Left" Margin="139,47,0,0" VerticalAlignment="Top"/>

        <!-- ListBoxes -->
        <ListBox x:Name="EIncomesList" ItemsSource="{Binding SelectedEIncomes}" SelectedItem="{Binding SelectedItem}" HorizontalAlignment="Left" Width="459" Height="213" VerticalAlignment="Top" Margin="45,68,0,0" Grid.ColumnSpan="2" Grid.RowSpan="2">
            <ListBox.ItemTemplate>
                <DataTemplate>
                    <StackPanel Orientation="Horizontal" Margin="5,0,0,0">
                        <Label Content="{Binding CVR}"/>
                        <Label Content="        "/>
                        <Label Content="{Binding PeriodStart}"/>
                        <Label Content="        "/>
                        <Label Content="{Binding PeriodEnd}"/>
                        <Label Content="        "/>
                        <Label Content="{Binding CreatedDate}"/>
                    </StackPanel>
                </DataTemplate>
            </ListBox.ItemTemplate>
        </ListBox>

        <!-- Images -->
        <Image Margin="0,93,21,-18" Source="/Images/Beierholm-logo.png" Stretch="Fill" HorizontalAlignment="Right" Width="212" Opacity="0.7" Grid.ColumnSpan="2" Grid.Row="1"/>

        <!-- Buttons -->
        <Button x:Name="Back" Content="Tilbage" HorizontalAlignment="Left" Margin="89,73,0,58" Click="Back_Click" Width="126" Grid.Row="1"/>
        <Button IsEnabled="False" x:Name="DownloadButton" Content="Download" HorizontalAlignment="Center" Margin="0,73,0,0" Width="120" Height="20" VerticalAlignment="Top" Grid.Row="1" Grid.Column="1"/>
        <Button IsEnabled="False" x:Name="ConvertButton" Content="Download (PDF)" HorizontalAlignment="Center" Margin="0,43,0,0" Width="120" Height="20" VerticalAlignment="Top" Grid.Column="1" Grid.Row="1"/>
        <Button x:Name="ShowEIncomeData" Command="{Binding ShowEIncomeData}" CommandParameter="{Binding}" Content="Åben EIncome" HorizontalAlignment="Left" Margin="89,43,0,0" VerticalAlignment="Top" RenderTransformOrigin="-0.042,-0.113" Width="126" Grid.Row="1"/>
    </Grid>
</Window>
